import statistics as st

data = [1, 2, 3, 4, 5, 6]
mean = st.mean(data)
mode = st.mode(data)
median = st.median(data)
variance = st.variance(data)
std = st.stdev(data)
 # when printing all the variables will display the calculations accordingly
